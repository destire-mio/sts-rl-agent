# E133：取消扩采，保留产物

用户要求修复“采样扩张先于学习证据”的推进方式。2026-09-22 08:26 上海时间，主任务向 E133 所属控制器发送终止信号；采集进程退出 143，拥有进程组清理通过，控制器退出 1。此次属于取消执行，不能计为游戏死亡或模拟器故障。项目继续推进，E133 不恢复。

保留 10,574 份分支文件，共 79,047,011 字节，并记录文件哈希。文件数不等于完整合格续局数。E133 缺少整轮完成和审计证明，部分数据没有进入训练；优化器更新为 0。新增 closed 输入检查阻止后续采集与训练入口启动；两个入口对实际关闭目录的拒绝经过验证。原登记源码与冻结运行时保留。

改进顺序：先用完成的 E128 数据检验学习方法；新采样需由明确的决策覆盖缺口与学习证据支持。发现事后获胜分支不足以证明网络能学会选择。E131 范围结果保留，但不再单凭范围试验触发大规模采样。

[取消与保存证明](e133-cancelled.json)。私有目录：`runs/heart-e133-early-card-learning-20260922-01`。E134 复用数据试验见 [结果](e134-existing-data.md)。
