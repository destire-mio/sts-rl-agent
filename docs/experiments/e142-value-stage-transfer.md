# E142 冻结价值模型在第二幕的迁移检查

E141首卡交叉验证失败后，固定两个模型及原家庭分组，使用既有E135/E128完整fit树，保持父策略的首幕Boss遗物选择，给其后第二幕第一份选牌评分。模型未见对应验证家庭的任何训练状态。没有训练、MCTS、新游戏或外部留出读取。

全1536家庭中1184有选牌节点，352在此前失败，保留原分母。每组仅33个菜单因超出首卡fit支持集回退父策略。root_only改变778次选牌，152胜，对照149；root_plus_later改变852次，150胜。相对父策略的描述性p分别.798153和1；两组均未过预设门槛。新增状态没有在这次第二幕迁移中建立收益依据；菜单回退没有覆盖大部分节点。

选中路线均从原文件核对哈希、完整终局标签及家族分组。此项是已有真实状态上的评分检查；没有实现第二幕接管策略或完成新的原生策略核验，不能称作自然开局部署成绩。结果未过门槛，不由此启动第二幕候选评估。

初始导出证明将尚未结束写入的stdout日志包含进哈希，最终打印只改变了probe.log。数据、模型、选择和报告哈希通过；主任务在进程退出0后创建sealed-completion.json封存最终日志和原证明。公开runner排除未关闭日志；没有重跑实验或修改选择结果。

[固定协议](e142-value-stage-transfer-protocol.json) · [结果与封存证明](e142-value-stage-transfer-result.json)。
