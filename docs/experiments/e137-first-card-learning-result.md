# E137 首卡模型：完成，未通过筛选

完整复用 E136 审核后的旧续局，没有新增采样。原1536个fit家庭按固定三折训练，父网络、战斗器和后续策略冻结。每折1000步，共3000步；不选择中间检查点。

交叉验证父策略149胜，候选155胜：共同胜126、新增29、丢失23、共同失败1358，净增6，配对描述性p=.488456。未通过预设净增30且p<.05的门槛；没有全量模型、历史留出评估、自然开发、未见验收或采用。

训练与核验阶段退出0，日志与完成证明哈希匹配，清理通过。1536个真实原生状态的卡牌身份、父动作、独立分数、实际选择与选中终局核验通过，查询不改状态/RNG。主任务按原始分支复算全部选中终局及配对统计。故障0。

此结果不支持将干预点移到第一张牌并保留小型冻结读出作为解决方案。它不证明早期选牌无用，也不把6局差值当作总体胜率提升。接下来停止同类读出的小幅变体，检查旧完整轨迹能否支持学习牌组状态的终局价值，并用未参与拟合家庭上的真实动作后果检验，不能用分类准确率替代。

[汇总证明](e137-first-card-learning-result.json) · [冻结设计](e137-first-card-learning-design.md)。原设计和准备记录保留其当时状态。
